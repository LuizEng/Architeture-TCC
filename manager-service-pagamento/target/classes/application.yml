spring:
  datasource:
    url: jdbc:mysql://localhost:3306/manager-pagamento
    username: root
    password: manager
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      
server:
  port: 8085      